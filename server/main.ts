import { Meteor } from "meteor/meteor";
import "../imports/api/todos/todosRpcMethods"; // Make sure this matches the file's casing
import "../imports/api/todos/todosPublications";

Meteor.startup(async () => {});
